/**
 * Community catalog helpers — backed by @astalakshimi/reference (shared JSON).
 * Subcaste/gotra are intentionally NOT in this catalog (free text on profiles).
 */
export {
  getCommunities,
  getCommunitiesForReligion,
  getCommunityLabelsForReligion,
  findCommunityBySlug,
  findCommunityByLabel,
  type CommunityEntry,
} from "@astalakshimi/reference"
